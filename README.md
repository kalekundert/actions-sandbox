Run interactive sessions of GitHub Actions nodes
================================================

Launch the workflow:

  Actions > Interactive session (left sidebar) > Run workflow > Run workflow

This workflow will continually output an SSH command.  Run that command to get 
interactive access.



